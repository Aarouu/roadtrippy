# roadtrippy
Platform for people to mark their favorite roadtrip routes 

## Features

* User can register and login
* User can put start and end to the route
* Algorithm searches the route between these points
* User can save the route to database and share with other users
* Users can rate other routes.
* 
## Insallation

Clone repo:

```
$ git clone https://github.com/Aarouu/roadtrippy.git
$ cd roadtrippy
```

Create venv:

```
$ python -m venv venv
$ source venv/bin/activate
```

Install requirements:

```
$ pip install flask
```


Voit käynnistää sovelluksen näin:

```
$ flask run
```
or 
```
$ python app.py
```
